server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/keinraf/EXAMEN-MICROSERVICIOS-KEVINRAF-KPS.git
          searchPaths: config-data
          default-label: principal
  security:
    user:
      name: kevinraf
      password: paricahua
